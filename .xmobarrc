Config { font = "-*-terminus-medium-*-*-*-12-*-*-*-*-*-*-*"
       , bgColor = "#202020"
       , fgColor = "#757575"
       , position = Top
       , lowerOnStart = True
       , commands = [ Run Date "%a %b %_d %Y-%m-%d :: %H:%M:%S" "date" 10
                    , Run StdinReader ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%StdinReader% }{ %date% "
       }
